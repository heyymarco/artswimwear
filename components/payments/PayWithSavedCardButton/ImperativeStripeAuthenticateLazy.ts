'use client'

// react:
import {
    // react:
    default as React,
}                           from 'react'



// react lazies:
export const ImperativeStripeAuthenticateLazy = React.lazy(() => import(/* webpackPrefetch: true */ './ImperativeStripeAuthenticate'));
