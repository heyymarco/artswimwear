auto reset validation on first load (if checkoutStep <= currentStep)
auto reset validation on checkoutStep change backward

[high prior] auto convert currentCurrency to USD if the currentCurrency is not supported by PayPal
